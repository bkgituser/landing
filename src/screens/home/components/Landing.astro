---
import { Trans } from 'astro-i18next/components'
import FullVideoBg from '../../../components/template/VideoBackground.astro'
import Loader from '@components/atom/Loader.astro'
---

<>
	<section id="hero" class="h-screen">
		<Loader />
		<FullVideoBg
			video="/assets/videos/landing.mp4"
			preload="meta"
			autoplay={true}
			id="landing-video"
		>
			<div class="h-3/4"></div>
			<div
				class="relative z-40 h-1/4 bg-[linear-gradient(180deg,_transparent_0%,_rgba(0,0,0,0.5)_100%)] px-8 md:px-32"
			>
				<swiper-container delay="3000" slides-per-view="1" speed="1500" autoplay="true" loop="true">
					<swiper-slide class="w-full">
						<h1 class="relative z-10 px-2 py-4 pt-2 text-2xl text-white lg:text-plusMid">
							<span class="mb-2 block w-12 border-2 border-white"></span>
							<Trans i18nKey="landing.carousel.title1">
								<strong>Smart Use</strong> of <strong>Advance Composites</strong>
							</Trans><br />
							<Trans i18nKey="landing.carousel.title11">
								to <strong>Boost Efficiency</strong> in <strong>Agriculture</strong>
							</Trans>
						</h1>
					</swiper-slide>
					<swiper-slide class="w-full">
						<h2 class="relative z-10 px-2 py-4 pt-2 text-2xl text-white lg:text-plusMid">
							<span class="mb-2 block w-12 border-2 border-white"></span>
							<strong
								><Trans i18nKey="landing.carousel.title2">
									Lightweight Sprayer Booms
								</Trans></strong
							><br />
							32m | 36m | 40m | 48m
						</h2>
					</swiper-slide>
				</swiper-container>
			</div>
		</FullVideoBg>
	</section>
	<script>
		const video = document.querySelector('#landing-video')
		const loader = document.querySelector('.loader-content')

		video.onloadeddata = function () {
			loader.classList.add('hidden')
		}
	</script>
</>
